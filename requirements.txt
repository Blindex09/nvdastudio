# Runtime — tinydb e httpx (com httpcore/h11/anyio/certifi/idna/
# typing_extensions, todos puro Python) sao vendorizados e VERSIONADOS em
# addon/globalPlugins/nvdastudio/lib/: o __init__.py do addon poe lib/ no
# sys.path, build.py empacota a pasta e tests/unit/
# test_build_runtime_dependencies.py garante que tudo vai no .nvda-addon e
# importa so com stdlib + lib/. Fora do NVDA (dev, CI) os pacotes do PyPI
# abaixo sao instalados por conveniencia da suite.
#
# Nada com extensao compilada entra em lib/ (pydantic_core, por exemplo):
# binario e preso a versao de Python/ABI do NVDA e quebraria em silencio.
# Para re-vendorizar:
#   python -m pip install --no-compile --target addon/globalPlugins/nvdastudio/lib httpx tinydb
#   (e apagar lib/bin/, que e so o script CLI do httpx)
tinydb>=4.8.0
httpx>=0.27.0

# Testes
pytest>=8.0.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
hypothesis>=6.100.0
python-dotenv>=1.0.0

# Seguranca (dev/CI apenas — auditoria de dependencias, ver .pre-commit-config.yaml)
pip-audit>=2.7.0
