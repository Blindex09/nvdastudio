tinydb>=4.8.0

# Runtime — no NVDA estas libs sao vendorizadas em
# addon/globalPlugins/nvdastudio/lib/ (fora do controle de versao). Fora do
# NVDA (dev, CI, testes) elas precisam vir do PyPI, senao os clientes de
# provedor (provider_client, ollama_client, opencode_go_client) degradam para
# "biblioteca HTTP interna nao esta disponivel" e ~29 testes falham.
httpx>=0.27.0
pydantic>=2.0.0

# Testes
pytest>=8.0.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
hypothesis>=6.100.0
python-dotenv>=1.0.0

# Seguranca (dev/CI apenas — auditoria de dependencias, ver .pre-commit-config.yaml)
pip-audit>=2.7.0
